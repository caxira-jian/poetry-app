# 2026-03-14 详情页返回逻辑

## 背景
- 项目运行在移动端浏览器中。
- 安卓系统返回、边缘返回以及 iOS 返回手势，如果不处理，可能直接让浏览器离开当前网页。

## 决策
- 详情页打开时压入一层应用内 history 状态。
- 当用户在详情页触发返回时，优先关闭详情页，回到应用内上一层，而不是直接退出网页。

## 原因
- 这是更接近原生应用的交互。
- 能减少误退出，提升移动端使用稳定性。

## 当前范围
- 当前只覆盖诗词详情页。

## 后续待观察点
- 在不同安卓浏览器和 iOS Safari 上的行为是否一致。
